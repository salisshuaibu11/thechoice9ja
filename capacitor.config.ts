import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'thechoice9ja',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
